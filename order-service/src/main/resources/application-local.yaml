spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/orders
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:password}
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest            # local default — not a real secret
  jpa:
    show-sql: true

logging:
  level:
    com.k8sdemo.order: DEBUG
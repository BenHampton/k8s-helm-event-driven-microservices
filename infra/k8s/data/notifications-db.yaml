apiVersion: v1
kind: Secret
metadata:
  name: notifications-db-credentials
  namespace: data
type: Opaque
stringData:
  POSTGRES_DB: "notifications"
  POSTGRES_USER: "notifications"
  POSTGRES_PASSWORD: "notifications"

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: notifications-db
  namespace: data
spec:
  serviceName: notifications-db
  selector:
    matchLabels:
      app: notifications-db
  template:
    metadata:
      labels:
        app: notifications-db
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          envFrom:
            - secretRef:
                name: notifications-db-credentials
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec: { command: ["pg_isready", "-U", "notifications"] }
            initialDelaySeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi

---

apiVersion: v1
kind: Service
metadata:
  name: notifications-db
  namespace: data
spec:
  clusterIP: None
  selector:
    app: notifications-db
  ports:
    - port: 5423
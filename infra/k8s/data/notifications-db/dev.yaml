apiVersion: v1
kind: Secret
metadata:
  name: notifications-db-dev-credentials
  namespace: data
type: Opaque
stringData:
  POSTGRES_DB: "notifications"
  POSTGRES_USER: "notifications"
  POSTGRES_PASSWORD: "notifications"

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: notifications-db-dev
  namespace: data
spec:
  serviceName: notifications-db-dev
  selector:
    matchLabels:
      app: notifications-db-dev
  template:
    metadata:
      labels:
        app: notifications-db-dev
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          envFrom:
            - secretRef:
                name: notifications-db-dev-credentials
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec: { command: ["pg_isready", "-U", "notifications"] }
            initialDelaySeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi

---

apiVersion: v1
kind: Service
metadata:
  name: notifications-db-dev
  namespace: data
spec:
  clusterIP: None
  selector:
    app: notifications-db-dev
  ports:
    - port: 5423
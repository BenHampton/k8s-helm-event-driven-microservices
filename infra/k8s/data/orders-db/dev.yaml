apiVersion: v1
kind: Secret
metadata:
  name: orders-db-dev-credentials
  namespace: data
type: Opaque
stringData: # everything is local - real world we'd use a secret manager
  POSTGRES_DB: "orders"
  POSTGRES_USER: "orders"
  POSTGRES_PASSWORD: "orders"

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: orders-db-dev
  namespace: data
spec:
  serviceName: orders-db-dev
  selector:
    matchLabels:
      app: orders-db-dev
  template:
    metadata:
      labels:
        app: orders-db-dev
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          envFrom:
            - secretRef:
                name: orders-db-dev-credentials
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec: { command: ["pg_isready", "-U", "orders"] }
            initialDelaySeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi

---

apiVersion: v1
kind: Service
metadata:
  name: orders-db-dev
  namespace: data
spec:
  clusterIP: None
  selector:
    app: orders-db-dev
  ports:
    - port: 5423
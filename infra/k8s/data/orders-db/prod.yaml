apiVersion: v1
kind: Secret
metadata:
  name: orders-db-prod-credentials
  namespace: data
type: Opaque
stringData: # everything is local - real world we'd use a secret manager
  POSTGRES_DB: "orders"
  POSTGRES_USER: "orders"
  POSTGRES_PASSWORD: "orders"

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: orders-db-prod
  namespace: data
spec:
  serviceName: orders-db-prod
  selector:
    matchLabels:
      app: orders-db-prod
  template:
    metadata:
      labels:
        app: orders-db-prod
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          envFrom:
            - secretRef:
                name: orders-db-prod-credentials
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec: { command: ["pg_isready", "-U", "orders"] }
            initialDelaySeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi

---

apiVersion: v1
kind: Service
metadata:
  name: orders-db-prod
  namespace: data
spec:
  clusterIP: None
  selector:
    app: orders-db-prod
  ports:
    - port: 5423